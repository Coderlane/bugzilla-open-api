get:
  summary: Get the version of the Bugzilla instance
  operationId: getVersion
  tags:
    - info
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            '$ref': '../types/info.yaml#/GetVersionResponse'
