get:
  summary: Get the current user.
  operationId: whoAmI
  tags:
    - info
    - users
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            '$ref': '../types/user.yaml#/User'
